#appointments_panel
  %h3
    = link_to '<', appointments_change_path(date: @date.prev_day, view: @view), remote: true
    = link_to '>', appointments_change_path(date: @date.next_day, view: @view), remote: true
    = @nav_title
  = agenda @date, @employees do |employee, hour|
    =''
:coffee
  jQuery ->
    $('.edit_hour').click ->
      #alert('foo')
      $(this).css('background-color', '#0d0')
      $(this).append('<div>hello world</div>')
