// TODO change to coffeescript
- if @appointment.nil?
  $('.appointment-dialog-error').css('display', 'inline');
- else
  -# TODO duplicate code
  -# TODO magic number offset
  $('#active-hour').prepend("#{j render('card', appointment: @appointment, hour: @hour, slot: @slot, offset: 105)}");
  InstallClientAppointmentHandler();
  HideClientAppointmentDialog();
